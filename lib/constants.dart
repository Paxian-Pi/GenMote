import 'package:flutter/material.dart';

class Constant {
  // Constant._();

  static String cardThemePref = "themeColor";
  static bool englishLang = true;
  static bool pidginEnglishLang = false;

  // App font sizes
  static const fontSizeLarger = 45.0;
  static const fontSizeLarge = 40.0;
  static const fontSizeMidUpper = 25.0;
  static const fontSizeMidLower = 20.0;
  static const fontSizeSmall = 18.0;
  static const fontSizeSmaller = 16.0;

  // Theme colors
  static const mainColor = Color(0xFF48BF53);
  static const accentColor = Color(0xFF91F086);
  static const accent = Color(0xFFA5D6A7);
  static const skyBlue = Color(0xFFE2E5FF);
  static const white = Color.fromARGB(255, 255, 255, 255);
  static const grey = Color.fromARGB(255, 211, 217, 210);
  static const darkGrey = Color.fromARGB(255, 150, 156, 149);
}